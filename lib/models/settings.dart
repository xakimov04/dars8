import 'package:flutter/material.dart';

class AppTheme {
  ThemeMode themeMode;

  AppTheme({
    required this.themeMode,
  });
}

class SizeText {
  double size;
  Color color;

  SizeText({
    required this.size,
    required this.color,
  });
}
